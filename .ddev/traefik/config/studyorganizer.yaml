#ddev-generated
# If you remove the ddev-generated line above you
# are responsible for maintaining this file. DDEV will not then
# update it, for example if you add `additional_hostnames`, etc.

http:
  routers:
    studyorganizer-web-80-http:
      entrypoints:
        - http-80
      rule: HostRegexp(`^studyorganizer\.ddev\.site$`)
      
      service: "studyorganizer-web-80"
      ruleSyntax: v3
      tls: false
      # middlewares:
      #   - "studyorganizer-redirectHttps"
    studyorganizer-web-8025-http:
      entrypoints:
        - http-8025
      rule: HostRegexp(`^studyorganizer\.ddev\.site$`)
      
      service: "studyorganizer-web-8025"
      ruleSyntax: v3
      tls: false
      # middlewares:
      #   - "studyorganizer-redirectHttps"
    studyorganizer-xhgui-80-http:
      entrypoints:
        - http-8143
      rule: HostRegexp(`^studyorganizer\.ddev\.site$`)
      
      service: "studyorganizer-xhgui-80"
      ruleSyntax: v3
      tls: false
      # middlewares:
      #   - "studyorganizer-redirectHttps"
    
    
    
    studyorganizer-web-80-https:
      entrypoints:
        - http-443
      rule: HostRegexp(`^studyorganizer\.ddev\.site$`)
      
      service: "studyorganizer-web-80"
      ruleSyntax: v3
      
      tls: true
      
    studyorganizer-web-8025-https:
      entrypoints:
        - http-8026
      rule: HostRegexp(`^studyorganizer\.ddev\.site$`)
      
      service: "studyorganizer-web-8025"
      ruleSyntax: v3
      
      tls: true
      
    
    studyorganizer-xhgui-80-https:
      entrypoints:
        - http-8142
      rule: HostRegexp(`^studyorganizer\.ddev\.site$`)
      
      service: "studyorganizer-xhgui-80"
      ruleSyntax: v3
      
      tls: true
      
    

  middlewares:
    studyorganizer-redirectHttps:
      redirectScheme:
        scheme: https
        permanent: true

  services:
    studyorganizer-web-80:
      loadbalancer:
        servers:
          - url: http://ddev-studyorganizer-web:80
        
    studyorganizer-web-8025:
      loadbalancer:
        servers:
          - url: http://ddev-studyorganizer-web:8025
        
    
    
    studyorganizer-xhgui-80:
      loadbalancer:
        servers:
          - url: http://ddev-studyorganizer-xhgui:80
        
    
    

tls:
  certificates:
    - certFile: /mnt/ddev-global-cache/traefik/certs/studyorganizer.crt
      keyFile: /mnt/ddev-global-cache/traefik/certs/studyorganizer.key